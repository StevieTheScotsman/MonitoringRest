apiVersion: v1
kind: Service
metadata:
  name: monitoring
  namespace: monitoring
spec:
  ports:
  - port: {{ .Values.service.port }}
    protocol: TCP
    targetPort: {{ .Values.service.port }}
  selector:
    app: monitoring
  type: ClusterIP